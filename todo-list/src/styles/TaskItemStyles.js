import { StyleSheet } from "react-native";

import { PRIMARY_COLOR, ACCENT_COLOR_YELLOW, ACCENT_COLOR_RED, TEXT_COLOR_DARK, TEXT_COLOR_DISABLED, BACKGROUND_COLOR_WHITE } from "../constants/styles";

export default Styles = StyleSheet.create({
    taskItem: {
        backgroundColor: BACKGROUND_COLOR_WHITE,
        paddingVertical: 12,
        paddingHorizontal: 15,
    },
});